<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="es.hol.smappdev.marvel_heroes_info.fragments.HeroDetailFragment"
    android:background="#d20303">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:id="@+id/hero_detail_name"
        android:layout_width="237dp"
        android:layout_height="36dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:gravity="center"
        android:text="@string/hello_blank_fragment"
        android:textAlignment="center"
        android:textColor="#FFF"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginTop="15dp"
        app:layout_constraintTop_toBottomOf="@+id/hero_detail_image"
        app:layout_constraintHorizontal_bias="0.503" />

    <TextView
        android:id="@+id/hero_detail_description"
        android:layout_width="239dp"
        android:layout_height="215dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="TextView"
        android:textAlignment="center"
        android:textColor="#FFF"
        app:layout_constraintLeft_toLeftOf="@+id/hero_detail_name"
        app:layout_constraintRight_toRightOf="@+id/hero_detail_name"
        app:layout_constraintTop_toBottomOf="@+id/hero_detail_name" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/hero_detail_image"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:layout_marginTop="24dp"
        app:layout_constraintLeft_toLeftOf="@+id/hero_detail_name"
        app:layout_constraintRight_toRightOf="@+id/hero_detail_name"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@mipmap/ic_launcher" />

</android.support.constraint.ConstraintLayout>
